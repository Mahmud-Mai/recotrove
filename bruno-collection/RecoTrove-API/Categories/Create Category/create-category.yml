info:
  name: Create Category
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/api/v1/categories"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{access_token}}"
  body:
    type: json
    data: |-
      {
        "name": "Programming"
      }
