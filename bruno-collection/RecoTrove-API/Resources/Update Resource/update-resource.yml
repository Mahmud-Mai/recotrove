info:
  name: Update Resource
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/api/v1/resources/{{resource_id}}"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{access_token}}"
  body:
    type: json
    data: |-
      {
        "title": "Python Crash Course (Updated)",
        "description": "An updated description for the Python resource"
      }
